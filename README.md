# AiBot
AI Document Assistant is a simple web app built with Streamlit and the Google Gemini API that lets users upload a document and ask questions about its contents. The app supports PDF and TXT files, uploads them to the Gemini API, and creates a chat interface where the AI answers questions using only the information from the uploaded document.
